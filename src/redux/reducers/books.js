import Loading from "../../Loading";

const initialState = {
  books: [],
  Loading: true
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
